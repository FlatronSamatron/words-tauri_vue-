{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the app windows",
  "windows": ["main", "game"],
  "permissions": [
    "core:default",
    "opener:default",
    "sql:default"
  ]
}
